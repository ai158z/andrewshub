name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python: '3.9'
          cache: 'pip'
          cache-dependency-path: requirements.txt

      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}

      - name: Install Dependencies
        run: |
          python -m pip install --no-deps -r requirements.txt

      - name: Lint with flake8 and bandit
        run: |
          python -m flake8 . --show-source
          python -m bandit -r . --severity-level=LOW

      - name: Run Tests
        run: |
          python -m pytest -v

      - name: Build
        run: |
          python setup.py build

      - name: Run All Tests
        run: |
          python -m pytest

      - name: Docker Build
        run: |
          docker build -t myapp:latest .

      - name: Run Lint and Format Check
        run: |
          flake8 . --show-source
          bandit -r . --severity-level=LOW

      - name: Run all tests
        run: |
          python -m pytest -v

    - name: Build
    run: |
      python setup.py sdist bdist_wheel

    - name: Run Docker build
    run: |
      docker build -t myapp:latest .

  needs: [ci]
  runs:
    - name: Cache Dependencies
    - uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python: '3.9'

    - name: Install Dependencies
      run: |
        python -m pip install --no-deps -r requirements.txt

    - name: Lint and format check
      run: |
        python -m flake8 . --show-source
        python -m bandit -r . --severity-level=LOW

    - name: Run Tests
      run: |
        python -m pytest -v

    - name: Build
      run: |
        python setup.py sdist bdist_wheel

    - name: Run Docker build
      run: |
        docker build -t myapp:latest .

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest .

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest .

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest .

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py sdist bdist_wheel

  - name: Run Docker build
    run: |
      docker build -t myapp:latest

  - name: Run all tests
    run: |
      python -m pytest -v

  - name: Build
    run: |
      python setup.py s