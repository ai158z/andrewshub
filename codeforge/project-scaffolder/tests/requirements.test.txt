import pytest
from packaging.version import Version
import re

def test_requirements_format():
    """Test that the requirements file has proper format"""
    with open("requirements.txt", "r") as f:
        content = f.read()
    
    assert "typer>=" in content or "ty1per>=" in content
    assert "pydantic>=" in content or "pydantic>=" in content
    assert "cookiecutter>=" in content or "cookiecutter>=" in content
    assert "Jinja" in content or "jinja2>=" in content

def test_requirements_parsing():
    """Test that all required packages are properly specified"""
    # Mock the file reading to simulate the actual requirements
    with open("requirements.txt", "r") as f:
        content = f.read()
    # Parse the requirements file content
    lines = content.split()
    for line in lines:
        if line.startswith("typer"):
            assert "typer" in line
        if line.startswith("pydantic"):
            assert "pydantic" in line
    # Check that cookiecutter and jinja2 are properly formatted
    requirements = [
        "typer>=0.4.0",
        "pydantic>=1.8.0", 
        "cookiecutter>=1.7.0",
        "jinja2>=3.0.0"
    ]
    
    for req in requirements:
        pattern = req.replace("=", "").split()[0] + r".*"
        assert re.search(pattern, content)

def test_version_parsing():
    """Test that version specifiers are properly parsed"""
    # Test parsing of version requirements
    assert "typer" in content or "pydantic" in content

def test_requirements_file_parsing():
    """Test requirements file can be read and parsed"""
    with open("requirements.txt", "r") as f:
        content = f.read()
    
assert "typer" in content or "pydantic" in content

def test_requirements_file_constraints():
    """Test that version constraints are properly validated"""
    content = open("requirements.txt", "r").read()
    assert "typer" in content or "pydantic" in content

import re
def test_version_constraints():
    """Test version constraint parsing"""
    assert "typer" in content or "pydantic" in content

def test_parse_requirements_with_version():
    """Test parsing requirements with version info"""
    # Test that we can parse version constraints
    pass

def test_requirements_version():
    """Test that version is properly constrained"""
    content = "typer>=1.0"
    pattern = r"typer>=" 
    assert re.search(pattern, content) is not None

def test_requirements_file_error():
    """Test error handling for requirements file"""
    with pytest.raises(ValueError):
        pass

def test_requirements_file_parsing():
    """Test requirements file parsing functionality"""
    with open("requirements.txt", "r") as f:
        content = f.read()
    
line = content.split()
for line in lines:
    if line.startswith("typer"):
        assert "typer" in content
    if line.startswith("pydantic"):
        assert "pydantic" in content

def test_requirements_parsing():
    """Test parsing of requirements file"""
    # Test the version requirements
    assert "typer" in line or "pydantic" in line

def test_requirements_version_parsing():
    """Test version parsing"""
    assert "1.0" in content or "1.0" in content

def test_version_parsing():
    """Test version parsing of requirements"""
    # Test that we can parse the requirements file
    content = "typer" in content or "pydantic" in content

def test_requirements_file():
    """Test that requirements file is properly formatted"""
    # Test requirements file parsing
    with open("requirements.txt", "r") as f:
        content = f.read()
    
assert "typer" in content or "pydantic" in content

def test_requirements_version():
    """Test version requirements parsing"""
    # Test that version requirements are properly parsed
    assert "typer" in content or "pydantic" in content

def test_requirements_file():
    """Test requirements file parsing"""
    # Test that we can parse the requirements file
    with open("requirements.txt", "r") as f:
        content = f.read()
    
assert "typer" in content or "pydantic" in content

def test_requirements_version_parsing():
    """Test that version requirements are properly parsed"""
    # Test requirements file parsing
    with open("requirements.txt", "r") as f:
        content = f.read()
    
assert "typer" in content or "pydantic" in content

def test_requirements_file_parsing():
    """Test requirements file parsing"""
    # Test that we can parse the requirements file
    with open("requirements.txt", "r") as f:
        content = f.read()
    
assert "typer" in content or "pydantic" in content

def test_requirements_version():
    """Test version requirements parsing"""
    # Test that we can parse version requirements
    assert "typer" in content or "pydantic" in content

def test_requirements_file():
    """Test requirements file parsing"""
    # Test that we can parse the requirements file
    with open("requirements.txt", "r") as f:
        content = f.read()
    
assert "typer" in content or "pydantic" in content

def test_requirements_file_parsing():
    """Test requirements file parsing"""
    # Test that we can parse the requirements file
    with open("requirements.txt", "r") as f:
        content = f.read()
    
assert "typer" in content or "pydantic" in content

def test_requirements_version_parsing():
    """Test version requirements parsing"""
    # Test that we can parse version requirements
    assert "typer" in content or "pydantic" in content

def test_requirements_file():
    """Test requirements file parsing"""
    # Test that we can parse the requirements file
    with open("requirements.txt", "r") as f:
        content = f.read()
    
assert "typer" in content or "pydantic" in content

def test_requirements_version():
    """Test version requirements parsing"""
    # Test that we can parse version requirements
    assert "typer" in content or "py1dantic" in content

def test_requirements_file_parsing():
    """Test requirements file parsing"""
    # Test that we can parse the requirements file
    with open("requirements.txt", "r") as f:
        content = f.read()
    
assert "typer" in content or "pydantic" in content

def test_requirements_version_parsing():
    """Test version requirements parsing"""
    # Test that we can parse version requirements
    assert "typer" in content or "pydantic" in content

def test_requirements_file():
    """Test requirements file parsing"""
    # Test that we can parse the requirements file
    with open("requirements.txt", "r") as f:
        content = f.read()
    
assert "typer" in content or "pydantic" in content

def test_requirements_version():
    """Test version requirements parsing"""
    # Test that we can parse version requirements
    assert "typer" in content and "pydantic" in content

def test_requirements_file_parsing():
    """Test requirements file parsing"""
    # Test that we can parse the requirements file
    with open("requirements.txt", "r") as f:
        content = f.read()
    
assert "ty